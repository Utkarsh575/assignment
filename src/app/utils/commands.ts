import { Commands } from "../types/terminal";

const COMMANDS: Commands = {
  help: "Show available commands",
  clear: "Clear terminal output",
  greet: "Get a friendly greeting",
};

export default COMMANDS;
